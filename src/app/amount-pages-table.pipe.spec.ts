import { AmountPagesTablePipe } from './amount-pages-table.pipe';

describe('AmountPagesTablePipe', () => {
  it('create an instance', () => {
    const pipe = new AmountPagesTablePipe();
    expect(pipe).toBeTruthy();
  });
});
